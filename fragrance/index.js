/**
 * @format
 */

import {AppRegistry} from 'react-native';
import NavegacionPrincipal from './src/Navigation/NavegacionPrincipal';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => NavegacionPrincipal);
